# Historical reference

`robot_dp_16.yaml` and its task definition are retained as supplied. They are not
loaded by the public CLI. The released ALOHA routes use 14-dimensional actions.
The matching code/checkpoint provenance for the 16D configuration is unknown;
no network or temporal sampling was changed to make that configuration run.
