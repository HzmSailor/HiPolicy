# Optional, only for authoring NEW language descriptions through Azure.
# Collection/evaluation use the bundled descriptions and do not need credentials.
AZURE_ENDPOINT=
AZURE_API_KEY=
WANDB_MODE=offline
